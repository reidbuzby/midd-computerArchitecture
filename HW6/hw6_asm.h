/*
This is a header file for the assembly code. It provides the function prototypes for each of the functions defined in the assembly so that the compiler knows how to properly call them.
*/

int avg(int x, int y);

int maxabs(int x, int y);

char lookup(char * str, int i);

int checksum(char * str);
